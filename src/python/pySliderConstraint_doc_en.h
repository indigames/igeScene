#pragma once
#include <Python.h>

// lowerLimit
PyDoc_STRVAR(SliderConstraint_lowerLimit_doc,
    "Lower limit vector. Default (0.0, 0.0, 0.0).\n"\
    "Type: Vec3\n"
);

// upperLimit
PyDoc_STRVAR(SliderConstraint_upperLimit_doc,
    "Upper limit vector. Default (0.0, 0.0, 0.0)\n"\
    "Type: Vec3\n"
);
